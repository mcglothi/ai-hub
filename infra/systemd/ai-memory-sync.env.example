# Optional: set explicit API key if you do not want to rely on Bitwarden session lookup.
# Secret reference: [Stored in Vaultwarden: PAT/AIKB Memory Core/API Key]
# MMC_API_KEY=replace_me

# Optional override if the service should point at a non-default endpoint.
# MEMORY_SYNC_SERVER=https://memory.home.timmcg.net
